import React from "react";

const Loading = () => {
  const lera = "";
  console.log(lera);
  return (
    <div
      style={{
        height: "80vh",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <h4>Loading...</h4>
    </div>
  );
};

export default Loading;
